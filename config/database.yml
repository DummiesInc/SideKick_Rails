# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: postgres
  password: 10prestige
  host: localhost
  port: 5433   # or 5432 if that’s your server port

development:
  <<: *default
  database: sidekick_v4

test:
  <<: *default
  database: sidekick_V4_test


production:
  primary:
    <<: *default
    database: sidekick_production
  cache:
    <<: *default
    database: sidekick_cache
  queue:
    <<: *default
    database: sidekick_queue
  cable:
    <<: *default
    database: sidekick_cable
